apiVersion: v1
kind: Service
metadata:
  name: akhq
  labels:
    app.kubernetes.io/instance: akhq
    app.kubernetes.io/name: akhq
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/instance: akhq
    app.kubernetes.io/name: akhq
  type: ClusterIP
