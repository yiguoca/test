namespace: eu-data-tools

resources:
  - ../../../../bases/kafka-strimzi/stage1/
  - resources/kafka-cluster.yaml

generatorOptions:
  disableNameSuffixHash: true
  labels:
    app.kubernetes.io/part-of: kafka

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/part-of: kafka
    photon/tier: infrastructure
    stage: kafka
